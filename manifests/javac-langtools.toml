# Full javac langtools coverage. Paths are relative to OpenJDK test/langtools.
#
# This intentionally includes areas previously excluded from default coverage,
# including annotation processing and modules, so failures can be classified.

[[group]]
id = "javac-all"
include = [
  "tools/javac",
]
tags = ["javac", "full-suite"]
